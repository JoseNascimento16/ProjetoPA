Em models.py

podemos criar os campos:

data_de_criacao = models.DateField(default=date.today, blank=True)
ou
data_de_criacao = models.DateTimeField(default=date.now, blank=True)
ou
data_de_criacao = models.DateField(null=True, blank=True)


Na views.py

from datetime import date

instancia_modelo = get_object_or_404(Modelo, pk=valor)

instancia_modelo.data_de_criacao = date.today()


E assim o valor do dia atual é salvo no modelo no banco de dados

###########################

# Check if date is in the allowed range (+4 weeks from today).

  if data > datetime.date.today() + datetime.timedelta(weeks=4):
	"display error"